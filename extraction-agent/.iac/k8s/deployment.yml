apiVersion: apps/v1
kind: Deployment
metadata:
  name: cezzis-cocktails-extraction-agent
  namespace: cezzis
  labels:
    app: cezzis-cocktails-extraction-agent
    app.kubernetes.io/part-of: cezzis
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: cezzis-cocktails-extraction-agent
  template:
    metadata:
      labels:
        app: cezzis-cocktails-extraction-agent
        app.kubernetes.io/part-of: cezzis
        app.kubernetes.io/managed-by: argocd
      annotations:
        dapr.io/enabled: "false"
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: cezzis-cocktails-extraction-agent
          image: acrveceusgloshared001.azurecr.io/cocktailsingestionextractionagent
          envFrom:
            - configMapRef:
                name: cezzis-cocktails-extraction-agent-config
            - secretRef:
                name: cezzis-cocktails-extraction-agent-shared-secrets